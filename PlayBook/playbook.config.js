// PlayBook Configuration
// Add folders where your chart images are stored.
// The server will search all these folders when looking for image files.
// No files are copied — images are served directly from their original location.

export default {
  chartFolders: [
    // Local charts folder (always included)
    './charts',
    'C:/Users/sspma/Dropbox/Gap Up Short Charts',
  ],
};
